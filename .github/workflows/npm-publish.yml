name: Legacy npm publish (manual only)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current release flow
        run: |
          echo "This repository ships desktop installers through the desktop release flow."
          echo "Automatic npm publish on GitHub release is disabled for this project."
